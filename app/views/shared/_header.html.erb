<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to image_tag('logo_header.png', alt: 'logo'), logged_in? ? main_path : root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class='nav-item'>
          <% if logged_in? %>
            <span class="navbar-text">
              <%= current_user.name %>
            </span>
            <%= link_to t('header.user.edit'), edit_user_path(current_user), class: 'btn btn-link nav-item' %>
            <%= link_to t('header.logout'), logout_path, data: { turbo_method: :delete }, class: 'btn btn-link nav-item' %>
          <% else %>
            <%= link_to t('header.login'), login_path, class: 'btn btn-link nav-item' %>
            <%= link_to t('header.user.new'), new_user_path, class: 'btn btn-link nav-item' %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
